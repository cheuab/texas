import './index.css';
import React from "react";
import { render } from "react-dom";
import { App } from "./App";
render(<App />, document.getElementById("root"));

// This file just connects your React (fancy) code to the HTML file (basic code but needed to run the app)